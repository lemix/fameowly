/**
 * Plugin stub — loaded when extension plugins directory is not present.
 * Exports an empty plugin array so the loader is a no-op.
 */

import type { Plugin } from "./types";

export const plugins: Plugin[] = [];
